jdk:
  - openjdk11

before_install:
  - sdk install java 11.0.10-open
  - sdk use java 11.0.10-open
  
install: 
  - FILE="-Dfile=pointsdk-release.aar" 
  - mvn install:install-file $FILE -DgroupId=co.areyouonpoint.pointsdk -DartifactId=pointsdk -Dversion=1.0.0 -Dpackaging=aar -DpomFile=pom.xml
